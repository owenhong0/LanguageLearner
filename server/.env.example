# Copy to server/.env for local dev:  cp .env.example .env
# NEVER commit a real key — server/.env is gitignored.

# Required: your Anthropic API key (read at runtime via process.env).
ANTHROPIC_API_KEY=

# Optional: Render sets PORT automatically in production; defaults to 8787 locally.
PORT=8787
